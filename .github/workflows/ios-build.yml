name: iOS Build

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: macos-14

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Test
        run: |
          echo "GitHub Actions работает!"
          sw_vers
          xcodebuild -version
