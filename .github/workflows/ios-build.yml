name: iOS Build

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: macos-15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Select Xcode
        run: |
          ls /Applications | grep Xcode
          sudo xcode-select -s /Applications/Xcode_26.0.app/Contents/Developer || true
          xcodebuild -version

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Generate Xcode project
        run: |
          xcodegen generate

      - name: Find project
        run: |
          PROJECT=$(find . -name "*.xcodeproj" -maxdepth 3 | head -n 1)
          echo "PROJECT=$PROJECT"

          xcodebuild -list -project "$PROJECT"

      - name: Build iOS Simulator
        run: |
          PROJECT=$(find . -name "*.xcodeproj" -maxdepth 3 | head -n 1)

          xcodebuild \
            -project "$PROJECT" \
            -scheme Luma \
            -sdk iphonesimulator \
            -destination 'platform=iOS Simulator,name=iPhone 16' \
            -derivedDataPath DerivedData \
            CODE_SIGNING_ALLOWED=NO \
            build

      - name: Find built app
        run: |
          find DerivedData -name "*.app" -type d -print

      - name: Package app
        run: |
          APP=$(find DerivedData -path "*Debug-iphonesimulator/*.app" -type d | head -n 1)

          mkdir -p artifact

          ditto -c -k --sequesterRsrc --keepParent \
            "$APP" \
            "artifact/FlowMessenger-simulator.zip"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: FlowMessenger-iOS
          path: artifact/FlowMessenger-simulator.zip
